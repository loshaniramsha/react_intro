// Corrected Customer class
export default class Customer {
    // Constructor with correct types
    constructor(
        public name: string,
        public email: string,
        public phone: string
    ) {}
}
